---
"@moonshot-ai/kimi-code": minor
---

The TUI now supports ssh:// remote workspaces: `kimi ssh://host/path` opens a session on the remote host (with a recent-workdirs picker for the ssh spec), `@` file mentions and path completion list remote files, and the new `/resume-remote` command acknowledges an interrupted, background-re-established ssh connection.
