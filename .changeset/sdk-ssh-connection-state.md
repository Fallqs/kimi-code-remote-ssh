---
"@moonshot-ai/kimi-code": minor
---

The SDK now exposes an ssh:// workspace's connection state (`getWorkspaceSshConnectionState`) and a manual resume (`resumeWorkspaceSshConnection`) that acknowledges a re-established connection (`blocked` → `ready`).
